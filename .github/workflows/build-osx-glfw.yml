name: Build OSX GLFW

on:
  workflow_dispatch

jobs:
  build:
    if: contains('["raub"]', github.actor)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 14.16
      - name: Get Npm Version
        id: package-version
        uses: martinbeentjes/npm-get-version-action@master
      - name: Build
        run: |
          sudo apt-get update -qq
          sudo apt-get install -qq xorg-dev
          chmod +x src/osx-glfw.sh && src/osx-glfw.sh
      - name: Store Binaries
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          draft: true
          tag_name: ${{ github.run }}-${{ github.sha }}
          name: Bin OSX GLFW ${{ github.sha }}
          body: OSX binaries ${{ steps.package-version.outputs.current-version }}
          files: src/build/*
